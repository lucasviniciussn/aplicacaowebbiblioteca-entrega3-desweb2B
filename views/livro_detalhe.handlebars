<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes do Livro</title>
    <link rel="stylesheet" href="/css/style.css"> 
</head>
<body>
    <header>
        <nav>
            <h1>Gestão de Bibliotecas</h1>
            <ul>
                <li><a href="/">Início</a></li>
                <li><a href="/livros">Livros</a></li>
                <li><a href="/autores">Autores</a></li>
                <li><a href="/categorias">Categorias</a></li>
                <li><a href="/acervo">Acervo</a></li>
                <li><a href="/reservas">Reservas</a></li>
                <li><a href="/bibliotecas">Bibliotecas</a></li>
                <li><a href="/usuarios">Usuários</a></li>
            </ul>
        </nav>
    </header>

    <main class="container">
        <h2>Editar Livro: {{livro.titulo}}</h2>
        
        <form id="formEdit">
            <div class="form-group">
                <label>Título da Obra:</label>
                <input type="text" name="nome" value="{{livro.titulo}}" required>
            </div>

            <div class="form-group">
                <label>Autores (Segure Ctrl para selecionar vários):</label>
                <select name="autoresIds" multiple required style="height: 100px;">
                    {{#each autores}}
                        <option value="{{this.id}}" {{#if (isIn this.id ../livro.autores)}}selected{{/if}}>
                            {{this.nome}}
                        </option>
                    {{/each}}
                </select>
            </div>

            <div class="form-group">
                <label>Categoria:</label>
                <select name="categoriaId" required>
                    <option value="">-- Selecione --</option>
                    {{#each categorias}}
                        <option value="{{this.id}}" {{#if (eq ../livro.categoriaId this.id)}}selected{{/if}}>
                            {{this.descricao}}
                        </option>
                    {{/each}}
                </select>
            </div>

            <div class="form-group">
                <label>Tags:</label>
                <input type="text" name="tags" value="{{livro.tags}}">
            </div>

            <div class="form-group" style="background-color: #f9f9f9; padding: 10px; border-radius: 5px;">
                <label>Quantidade Total no Acervo:</label>
                <input type="number" name="quantidade_total" value="{{livro.quantidade_total}}">
                <p style="font-size: 0.8em; color: #666; margin-top: 5px;">
                    Alterar este número criará ou removerá itens do acervo automaticamente.
                </p>
            </div>

            <div class="actions" style="margin-top: 20px;">
                <button type="submit">Salvar Alterações</button>
                <button type="button" class="delete" style="background-color: #e74c3c; margin-left: 10px;" onclick="deletar()">Excluir Livro</button>
            </div>
        </form>
    </main>

    <script>
        const id = "{{livro.id}}";
        
        document.getElementById('formEdit').addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const data = Object.fromEntries(formData.entries());
            
            data.autoresIds = formData.getAll('autoresIds');

            try {
                const res = await fetch('/livros/'+id, {
                    method: 'PUT',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify(data)
                });
                
                if(res.ok) { 
                    alert('Livro atualizado com sucesso!'); 
                    window.location.reload(); 
                } else { 
                    const err = await res.json(); 
                    alert('Erro: ' + (err.error || 'Falha ao atualizar')); 
                }
            } catch (error) {
                alert('Erro de conexão: ' + error.message);
            }
        });

        async function deletar() {
            if(!confirm('Tem certeza? Se houver cópias físicas deste livro no acervo, a exclusão poderá ser bloqueada dependendo das regras.')) return;
            
            try {
                const res = await fetch('/livros/'+id, { method: 'DELETE' });
                
                if(res.ok) {
                    window.location.href = '/livros';
                } else {
                    const err = await res.json();
                    alert('Erro ao deletar: ' + (err.error || 'Erro desconhecido'));
                }
            } catch (error) {
                alert('Erro de conexão: ' + error.message);
            }
        }
    </script>
</body>
</html>