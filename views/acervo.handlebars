<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Acervo Físico</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <nav>
            <h1>Gestão de Bibliotecas</h1>
            <ul>
                <li><a href="/">Início</a></li>
                <li><a href="/livros">Livros</a></li>
                <li><a href="/autores">Autores</a></li>
                <li><a href="/categorias">Categorias</a></li>
                <li><a href="/acervo">Acervo</a></li>
                <li><a href="/reservas">Reservas</a></li>
                <li><a href="/bibliotecas">Bibliotecas</a></li>
                <li><a href="/usuarios">Usuários</a></li>
            </ul>
        </nav>
    </header>

    <main class="container">
        <h2>Nova Cópia Física</h2>
        <form id="formAcervo">
            <div class="form-group">
                <label>Selecione o Livro:</label>
                <select name="livroId" required>
                    <option value="">-- Escolha um livro --</option>
                    {{#each livros}}
                        <option value="{{this.id}}">{{this.titulo}}</option>
                    {{/each}}
                </select>
            </div>
            
            <div class="form-group">
                <label>Selecione a Biblioteca:</label>
                <select name="bibliotecaId" required>
                    <option value="">-- Escolha uma biblioteca --</option>
                    {{#each bibliotecas}}
                        <option value="{{this.id}}">Bib ID {{this.id}} (CNPJ: {{this.cnpj}})</option>
                    {{/each}}
                </select>
            </div>

            <div class="form-group">
                <label>Quantidade de Cópias:</label>
                <input type="number" name="quantidade" min="1" value="1" required>
            </div>

            <button type="submit">Adicionar ao Acervo</button>
        </form>

        <h2 style="margin-top: 40px;">Cópias no Acervo</h2>
        <p style="margin-bottom: 20px; color: #666;">Clique em um item para editar ou excluir.</p>
        
        <table>
            <thead>
                <tr>
                    <th>ID Cópia</th>
                    <th>Livro</th>
                    <th>Biblioteca (CNPJ)</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                {{#each acervo}}
                <tr onclick="window.location.href='/acervo/{{this.id}}'" style="cursor: pointer;">
                    <td>{{this.id}}</td>
                    <td>{{this.nomeLivro}}</td>
                    <td>{{this.cnpjBiblioteca}}</td>
                    
                    <td class="{{#if (eq this.status 'Disponível')}}status-livre{{else}}status-ocupado{{/if}}">
                        {{this.status}}
                    </td>
                </tr>
                {{else}}
                <tr>
                    <td colspan="4" style="text-align:center">Nenhum item no acervo. Adicione livros acima.</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </main>

    <script>
        document.getElementById('formAcervo').addEventListener('submit', async (e) => {
            e.preventDefault();
            const data = Object.fromEntries(new FormData(e.target));
            
            try {
                const res = await fetch('/acervo', {
                    method: 'POST', 
                    headers: {'Content-Type': 'application/json'}, 
                    body: JSON.stringify(data)
                });
                
                if(res.ok) {
                    window.location.reload();
                } else {
                    const err = await res.json();
                    alert('Erro ao criar cópia: ' + (err.error || 'Desconhecido'));
                }
            } catch (error) {
                 if(!error.message.includes('JSON')) alert('Erro: ' + error.message);
                 else window.location.reload(); 
            }
        });
    </script>
</body>
</html>